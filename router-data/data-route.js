const express = require('express');

//database access using knex
const knex = require('./data/dbConfig.js.js');
// Invoke Router
const router = express.Router();

server.use('/', (req, res) => )

//Database handlers and CRUD operations-------------


//Export Router 
module.exports = router;